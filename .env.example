# Copy to .env for local dev (optional — defaults to http://localhost:5000).
# On Vercel, set VITE_API_URL as a Project Environment Variable pointing to
# your deployed Render backend, e.g. https://h2-softskills-server.onrender.com

VITE_API_URL=http://localhost:5000
